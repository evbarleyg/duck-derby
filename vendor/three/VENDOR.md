three.js r160 (0.160.0), MIT licence — vendored from the npm package `three@0.160.0`
(build/three.module.min.js and examples/jsm/utils/BufferGeometryUtils.js) so the
game runs without a CDN. See LICENSE.

../es-module-shims.js: es-module-shims 1.10.0 (MIT, Guy Bedford) — import-map polyfill for older
browsers, vendored from the npm package `es-module-shims@1.10.0` (dist/es-module-shims.js).

../qrcode.js: qrcode-generator 1.4.4 (MIT, Kazuhiko Arase) — QR code encoder used for the join code
on the lobby screen, vendored from the npm package `qrcode-generator@1.4.4` (qrcode.js).
